<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.usjt.wesley.exerciciojson" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!-- Usar internet -->
        <uses-permission android:name="android.permission.INTERNET" />

        <!-- Verifica se existe conexão com internet -->
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
        <activity
            android:name="br.usjt.wesley.exerciciojson.controller.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="br.usjt.wesley.exerciciojson.controller.ListaVooActivity"
            android:label="@string/title_activity_lista_voo" >
        </activity>
        <activity
            android:name="br.usjt.wesley.exerciciojson.controller.DetalheVooActivity"
            android:label="@string/title_activity_detalhe_voo" >
        </activity>
    </application>

</manifest>
